{
  "name": "understudy-consulting-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "verify": "npm test && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "htmlparser2": "^10.1.0"
  }
}
